export function taskFirst() {
	const = "I prefer const when I can.";
	return task;
}

export function getLast() {
	return ' is okay';
}

export function taskMext() {
	let combination = 'But sometimes let';
	combination += getLast();

	return combination;
}

